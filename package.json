{
  "name": "asteris-automation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "build": "npm run install:all && npm run build:frontend && npm run build:backend",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install --build-from-source=@tensorflow/tfjs-node",
    "build:frontend": "cd frontend && npm run build && mkdir -p ../backend/frontend && cp -r out/* ../backend/frontend/",
    "build:backend": "cd backend && npm run build",
    "start": "cd backend && node dist/index.js",
    "postinstall": "echo 'Skipping postinstall in root package.json'"
  },
  "devDependencies": {
    "@types/react-dom": "^19.1.5",
    "concurrently": "^8.0.1",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "lucide-react": "^0.511.0",
    "serve": "^14.2.1"
  },
  "optionalDependencies": {
    "@tensorflow/tfjs-node": "^4.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
